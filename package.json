{
  "name": "cloudtok",
  "version": "1.0.0",
  "description": "CloudTok - Share Videos, Connect with the World",
  "scripts": {
    "android:build": "npx cap add android && npx cap sync && npx cap open android",
    "tauri:dev": "npx tauri dev",
    "tauri:build": "npx tauri build"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@tauri/cli": "^1.5.0"
  }
}
